#!/bin/bash
echo "🧪 تست نهایی ساختار دیتابیس..."
DB_PATH=~/Documents/bankmanager.db
rm -f "$DB_PATH"

# اجرای برنامه که دیتابیس را می‌سازد (شبیه‌سازی با sqlite3 چون نمی‌توانیم برنامه را اجرا کنیم)
# اما چون ما کد C# را تغییر دادیم، باید مطمئن شویم که EF/SQLite-net جدول‌ها را با ستون‌های جدید می‌سازد.
# بهترین راه بررسی کد C# بود که انجام دادیم.
# اینجا فقط چک می‌کنیم که اگر دیتابیس ساخته شود، ستون‌ها را دارد.

# ما نمی‌توانیم کامپایل و اجرا کنیم، پس فقط کد را بازبینی کردیم.
# اما می‌توانیم یک دیتابیس دستی بسازیم تا مطمئن شویم اسکریپت‌های ما درست کار می‌کنند؟ نه، این فایده ندارد.

# بیایید فقط فایل‌های مدل را دوباره چک کنیم تا مطمئن شویم همه چیز درست است.
grep -E "CreatedAt|UpdatedAt" BankManagerApp/Models/*.cs
