<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BankManagerApp.Views.DateTimePickerDialog"
             Title="انتخاب تاریخ و ساعت"
             BackgroundColor="#80000000">
    
    <Border BackgroundColor="White"
            StrokeShape="RoundRectangle 15"
            StrokeThickness="0"
            Padding="20"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            WidthRequest="350">
        
        <VerticalStackLayout Spacing="15">
            
            <!-- Title -->
            <Label Text="📅 انتخاب تاریخ و ساعت" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"
                   TextColor="#333"/>
            
            <BoxView HeightRequest="1" Color="#DDD" Margin="0,5"/>
            
            <!-- Year Picker -->
            <Grid ColumnDefinitions="100,*" ColumnSpacing="10">
                <Label Grid.Column="0" Text="سال:" VerticalOptions="Center" TextColor="#666"/>
                <Picker Grid.Column="1" x:Name="YearPicker" 
                        BackgroundColor="#F5F5F5" 
                        TextColor="#333"
                        SelectedIndexChanged="OnDateChanged"/>
            </Grid>
            
            <!-- Month Picker -->
            <Grid ColumnDefinitions="100,*" ColumnSpacing="10">
                <Label Grid.Column="0" Text="ماه:" VerticalOptions="Center" TextColor="#666"/>
                <Picker Grid.Column="1" x:Name="MonthPicker" 
                        BackgroundColor="#F5F5F5" 
                        TextColor="#333"
                        SelectedIndexChanged="OnDateChanged"/>
            </Grid>
            
            <!-- Day Picker -->
            <Grid ColumnDefinitions="100,*" ColumnSpacing="10">
                <Label Grid.Column="0" Text="روز:" VerticalOptions="Center" TextColor="#666"/>
                <Picker Grid.Column="1" x:Name="DayPicker" 
                        BackgroundColor="#F5F5F5" 
                        TextColor="#333"/>
            </Grid>
            
            <!-- Time Selection -->
            <Grid ColumnDefinitions="100,*,Auto,*" ColumnSpacing="10">
                <Label Grid.Column="0" Text="ساعت:" VerticalOptions="Center" TextColor="#666"/>
                <Entry Grid.Column="1" x:Name="HourEntry" 
                       Placeholder="00" 
                       Keyboard="Numeric" 
                       MaxLength="2"
                       BackgroundColor="#F5F5F5" 
                       TextColor="#333"
                       HorizontalTextAlignment="Center"/>
                <Label Grid.Column="2" Text=":" VerticalOptions="Center" FontSize="20" TextColor="#666"/>
                <Entry Grid.Column="3" x:Name="MinuteEntry" 
                       Placeholder="00" 
                       Keyboard="Numeric" 
                       MaxLength="2"
                       BackgroundColor="#F5F5F5" 
                       TextColor="#333"
                       HorizontalTextAlignment="Center"/>
            </Grid>
            
            <BoxView HeightRequest="1" Color="#DDD" Margin="0,5"/>
            
            <!-- Buttons -->
            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="الان" 
                        Clicked="OnNowClicked"
                        BackgroundColor="#E8EAF6"
                        TextColor="#512BD4"
                        CornerRadius="8"/>
                <Button Grid.Column="1" 
                        Text="لغو" 
                        Clicked="OnCancelClicked"
                        BackgroundColor="#F5F5F5"
                        TextColor="#666"
                        CornerRadius="8"/>
                <Button Grid.Column="2" 
                        Text="تایید" 
                        Clicked="OnConfirmClicked"
                        BackgroundColor="#512BD4"
                        TextColor="White"
                        CornerRadius="8"/>
            </Grid>
            
        </VerticalStackLayout>
    </Border>
    
</ContentPage>
