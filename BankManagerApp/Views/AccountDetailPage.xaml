<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BankManagerApp.Views.AccountDetailPage"
             FlowDirection="RightToLeft"
             BackgroundColor="#F8F9FA"
             Title="جزئیات کیف پول">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="حذف" Order="Primary" Priority="0" Clicked="OnDeleteWalletClicked"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Account Header -->
            <Border StrokeShape="RoundRectangle 20" BackgroundColor="#512BD4" StrokeThickness="0" Padding="20">
                <VerticalStackLayout Spacing="10">
                    <Label x:Name="AccountNameLabel" Text="نام کیف پول" TextColor="White" FontSize="16" Opacity="0.9"/>
                    <Label x:Name="BalanceLabel" Text="0 تومان" TextColor="White" FontSize="32" FontAttributes="Bold"/>
                </VerticalStackLayout>
            </Border>

            <!-- New Transaction Section -->
            <Border StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0" Padding="15">
                <VerticalStackLayout Spacing="15">
                    <Label Text="ثبت تراکنش جدید" FontAttributes="Bold" FontSize="16" TextColor="#333"/>
                    
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Grid.Column="0" x:Name="IncomeTab" Text="💰 واریز" Clicked="OnTransactionTypeChanged" BackgroundColor="#E8F5E9" TextColor="#2E7D32" CornerRadius="10" FontAttributes="Bold"/>
                        <Button Grid.Column="1" x:Name="ExpenseTab" Text="💸 هزینه" Clicked="OnTransactionTypeChanged" BackgroundColor="#F5F5F5" TextColor="#666" CornerRadius="10" FontAttributes="Bold"/>
                    </Grid>

                    <Entry x:Name="AmountEntry" Placeholder="مبلغ (تومان)" Keyboard="Numeric" BackgroundColor="#F5F5F5" TextColor="#333"/>
                    
                    <Picker x:Name="CategoryPicker" Title="انتخاب دسته‌بندی" BackgroundColor="#F5F5F5" TextColor="#333">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>حقوق</x:String>
                                <x:String>واریز ریالی</x:String>
                                <x:String>دنگ</x:String>
                                <x:String>نقدی</x:String>
                                <x:String>سایر</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>

                    <Entry x:Name="DescriptionEntry" Placeholder="توضیحات (اختیاری)" BackgroundColor="#F5F5F5" TextColor="#333"/>

                    <Button Text="ثبت تراکنش" Clicked="OnSubmitTransactionClicked" BackgroundColor="#512BD4" TextColor="White" CornerRadius="10" FontAttributes="Bold"/>
                </VerticalStackLayout>
            </Border>

            <!-- Transaction History Header -->
            <Label Text="تاریخچه تراکنش‌ها" FontSize="16" FontAttributes="Bold" TextColor="#333"/>
            
            <!-- Transactions List (BindableLayout) -->
            <StackLayout x:Name="TransactionsContainer" Spacing="10">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 10" BackgroundColor="White" StrokeThickness="0" Padding="10">
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <Label Grid.Column="0" Text="{Binding Type, Converter={StaticResource TransactionIconConverter}}" FontSize="24" VerticalOptions="Center" Margin="0,0,10,0"/>
                                
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                    <Label Text="{Binding Category}" FontAttributes="Bold" TextColor="#333"/>
                                    <Label Text="{Binding DateTime, StringFormat='{0:yyyy/MM/dd HH:mm}'}" FontSize="12" TextColor="#888"/>
                                </VerticalStackLayout>
                                
                                <Label Grid.Column="2" Text="{Binding Amount, StringFormat='{0:N0}'}" FontAttributes="Bold" TextColor="{Binding Type, Converter={StaticResource TransactionColorConverter}}" VerticalOptions="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </StackLayout>
            
            <!-- Empty State Label (Visible when no transactions) -->
            <Label x:Name="EmptyStateLabel" 
                   Text="هنوز تراکنشی ثبت نشده" 
                   HorizontalOptions="Center" 
                   TextColor="#888" 
                   IsVisible="False"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
